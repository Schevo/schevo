from setuptools import setup, find_packages

setup(
    name=${repr(project)|empty},
    version=${repr(version)|empty},
    #description="",
    #author="",
    #author_email="",
    #url="",
    install_requires=[
    "Schevo==dev,>=3.0b2dev-r1763",
    ],
    packages=find_packages(),
    include_package_data=True,
    package_data={},
    entry_points="""
    """,
)
